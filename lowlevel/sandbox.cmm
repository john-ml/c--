#define a3 1
#define b 3
#define paste(x, y) x ## y
#define IF #if
#define ELSE #else
#define ENDIF #endif
#define try(a, b) IF paste(a, b) == 1
try(a, b)
  true
ELSE
  false
ENDIF

#if (a3 != 0) && (b != 0)
  true
#else
  false
#endif
